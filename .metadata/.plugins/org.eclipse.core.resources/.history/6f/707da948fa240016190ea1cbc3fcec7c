package flow.token;

import java.util.regex.Pattern;

public abstract class Token {
	public abstract TokenType getType();
	public abstract String getData();
	private String[] keyLiterals = new String[]{"null","false","true"};
	
	/**
	 * Tokenize a String. The logic is as follows:
	 * 
	 * <ol>
	 * 	<li></li>
	 * </ol>
	 * 
	 * @return array of Tokens
	 */
	public static Token[] tokenize(String s) {
		StringBuilder data = new StringBuilder(s);
		ArrayList<Token> tokens = new ArrayList<Token>();
		// Begin by scanning for keywords \\
			// Scan for keyword literals
			for (String literal: keyLiterals) {
				
			}
		return null;
	}
	
	private char firstChar(String s) {
		return s.charAt(0);
	}
}
